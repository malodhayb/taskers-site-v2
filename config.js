/* ====================================================================
   Fill these in with your own Supabase project's values to enable real
   sign-up / login. Get them from: Supabase dashboard -> Project Settings
   -> API -> "Project URL" and "anon public" key.

   Leave them as-is (untouched) and the app will keep working with its
   built-in demo accounts instead of real accounts — nothing breaks.
   ==================================================================== */
window.SUPABASE_URL = "https://mubbmuxakzgjvsjquuiz.supabase.co";
window.SUPABASE_ANON_KEY = "sb_publishable_PHdi7BrYQJd2RPJ8418jHg_xscC4XfG";
window.SUPABASE_CONFIGURED = window.SUPABASE_URL.startsWith("http") && window.SUPABASE_ANON_KEY.length > 20;
